services:
  - type: web
    name: fake-news-detector
    env: python
    buildCommand: pip install -r Back-End/requirements.txt
    startCommand: cd Back-End && gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: PORT
        value: 10000 